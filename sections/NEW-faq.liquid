{%- if section.blocks.size > 0 -%}
  <div class="section {%- if section.settings.section-type == "boxed" -%}section-boxed{%- endif -%} {%- if section.settings.section-color == "dark" -%}bg-custom{%- endif -%}">
    <div class="section-stack section-stack--horizontal ">
      {%- content_for 'blocks' -%}
    </div>
  </div>
{%- endif -%}

{% schema %}
  {
    "name": "NEW - FAQ",
    "class": "faq_box card-slider",
    "settings": [
      {
        "type": "select",
        "id": "section-type",
        "label": "Section type",
        "options": [
          {
            "value": "boxed",
            "label": "Boxed"
          },
          {
            "value": "full-width",
            "label": "Full width"
          }
        ],
        "default": "boxed"
      },
      {
        "type": "select",
        "id": "section-color",
        "label": "Section background",
        "options": [
          {
            "value": "dark",
            "label": "Dark"
          },
          {
            "value": "light",
            "label": "Light"
          }
        ],
        "default": "dark"
      }
    ],
    "tag": "div",
    "blocks": [{"type": "@theme"}],
    "presets":[
      {
        "name": "NEW - FAQ"
      }
    ]
  }
{% endschema %}